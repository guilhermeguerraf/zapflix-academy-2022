<div class="d-flex justify-content-between align-items-center">
  <h1>Directors</h1>
  <%= link_to "+ New", new_director_path, class: "btn btn-primary" %>
</div>

<table id="directors" class="table table-striped table-dark">
  <thead>
    <tr>
      <th>Nome</th>
      <th>Criado em</th>
      <th>Ações</th>
    </tr>
  </thead>
  <tbody>
    <% @directors.each do |director| %>
      <%= render director %>
    <% end %>
  </tbody>
</table>
