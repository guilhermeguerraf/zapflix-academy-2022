<div class="d-flex justify-content-between align-items-center">
  <h1>Artists</h1>
  <%= link_to "+ New", new_artist_path, class: "btn btn-primary" %>
</div>

<table id="artists" class="table table-striped table-dark">
  <thead>
    <tr>
      <th>Nome</th>
      <th>Criado em</th>
      <th>Ações</th>
    </tr>
  </thead>
  <tbody>
    <% @artists.each do |artist| %>
      <%= render artist %>
    <% end %>
  </tbody>
</table>
